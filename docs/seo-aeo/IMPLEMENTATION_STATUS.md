# SEO/AEO Implementation Status

Update this file after every approved implementation session.

## Current strategic decision

- Primary position: **APPROVED (DEC-001, 2026-07-28)** — "BBTx Consulting helps leaders and organizations make better decisions, align their people, and turn strategy into sustained execution — guided directly by Dr. Grant Tate's decades of executive leadership, organizational transformation, and coaching experience. AI is an enabling capability, not the category." (see `OPEN_DECISIONS.md`)
- AI role: Enabling capability, not primary category
- Measurement baseline date: **2026-07-30** — manual GSC export reviewed (see `BASELINE_2026-07-30.md`). Note: GSC index data goes back to 2026-04-30, contradicting the earlier "no property exists yet" assumption — origin unconfirmed, see `OPEN_DECISIONS.md` OPEN-008.
- Current implementation phase: Phase 0 controls done; Phase 1 entity correction underway (GBP live edits applied 2026-09-04, Chamber/LinkedIn corrections drafted and paste-ready, gated on account access/Grant); Phase 5 AEO-002 baseline established 2026-09-04

## Status definitions

- `NOT STARTED`
- `PLANNING`
- `BLOCKED - HUMAN GATE`
- `IN PROGRESS`
- `READY FOR REVIEW`
- `DEPLOYED - VERIFYING`
- `DONE`
- `DEFERRED`

## Active work

| Task ID | Status | Branch/PR | Owner | Manual gate | Last update | Next action |
|---|---|---|---|---|---|---|
| GOV-001 | DONE | (uncommitted) | Claude | | 2026-07-28 | Root `CLAUDE.md` created, imports the addendum |
| GOV-002 | DONE | (uncommitted) | Claude + Mel | | 2026-07-28 | Audit/traceability/backlog/decisions/gates files produced this session |
| DEC-001 | DONE | | Grant + Mel | | 2026-07-28 | Positioning statement approved; wording drafts for POS-002/AUT-002/SER-00x still need their own review before publishing |
| DAT-004 | DONE | (uncommitted) | Claude | | 2026-07-28 | Confirmed zero existing analytics/consent implementation — nothing to conflict with |
| TECH-001 | IN PROGRESS | (uncommitted) | Claude | | 2026-07-28 | Technical schema/metadata/sitemap fixes — see `REPO_IMPLEMENTATION_BACKLOG.md` Phase 0 |
| DAT-001 | IN PROGRESS | (uncommitted) | Mel | H-003 (partial) | 2026-07-30 | GA4 property created, measurement ID `G-WX6WWDEBB6` wired into `app/components/GoogleAnalytics.tsx` via root layout. GSC property confirmed live (index data since 2026-04-30 — see OPEN-008); no events/conversions instrumented yet (DAT-002/CON-002) |
| DAT-003 | IN PROGRESS | (docs only) | Mel + Claude | | 2026-07-30 | 3-month manual GSC export (Coverage + Performance) pulled and summarized — see `BASELINE_2026-07-30.md`. Full 12-16 month range and recurring pulls pending DAT-005 |
| DAT-005 | IN PROGRESS | (uncommitted) | Claude + Mel | H-003 (GCP OAuth setup) | 2026-09-04 | GSC half resolved: **2026-09-03/04, GSC read access established** via a Search Console MCP server — OAuth as `devmelo2003@gmail.com`, `siteRestrictedUser` scope, on `sc-domain:bbtx.ai`. Sidesteps the Workspace-account GCP Console block entirely (no OAuth client created inside Mel/Grant's restricted account — see `OPEN_DECISIONS.md` OPEN-004 resolution). First API-based pull performed 2026-09-04 (`GSC_HEALTH_2026-09-04.md`), feeding TECH-005/AEO-001. GA4 Data API access for internal reporting is untouched — that half of DAT-005 stays open |
| TECH-002 | READY FOR REVIEW | (uncommitted) | Claude | | 2026-07-31 | Mel confirmed `www.bbtx.ai` as canonical (OPEN-009 resolved). Every hardcoded `https://bbtx.ai` reference across the repo (metadataBase, sitemap, robots, all page canonicals/OG/JSON-LD `@id`s, transactional email templates) updated to `https://www.bbtx.ai`. `npx tsc --noEmit` + `npx eslint` clean on all touched files (1 pre-existing unrelated error in `links/page.tsx`, confirmed via `git diff` not touched by this change) |
| AUD-001 | DONE | (uncommitted) | Claude | | 2026-08-01 | Homepage-focused SEO/AEO audit performed by direct code inspection. All 3 high-impact and all 4 medium findings addressed same pass (see ENT-005/TECH-003 below) |
| TECH-003 (new) | DONE | (uncommitted) | Claude | | 2026-08-01 | Medium-severity audit fixes: (1) `app/sections/Services.tsx` — 5 service names (4 cards + featured) promoted from `<p>` to `<h3>` under the section's existing `<h2>`; (2) `app/sections/Advantage.tsx` — heading hierarchy un-inverted: "Start with People" (photo caption) demoted `<h2>`→`<h3>`, the section's actual thesis line promoted `<p>`→`<h2>`, 3 benefit-row titles promoted `<p>`→`<h3>`; (3) `app/sitemap.ts` — removed fabricated `lastModified: new Date()` from all 20 static entries (was sending "changed right now" on every crawl); blog entries keep their real `updated_at` date, unchanged; (4) `app/sections/Results.tsx` — removed `unoptimized` from the 38 logo-carousel `<Image>` renders, restoring Next's AVIF/WebP optimization (confirmed enabled in `next.config.ts`). `npx tsc --noEmit` + `npx eslint` clean on all 4 files (1 pre-existing unrelated apostrophe-escaping error in `Advantage.tsx`, confirmed via `git diff` — same string, only the tag name changed) |
| TECH-004 (new) | DONE | (uncommitted) | Claude | | 2026-08-31 | GSC "Page indexing" 404 report (32 URLs) triaged. Root-caused 2 as genuinely dead legacy URLs from the 2026-07-20 redesign (`78ef728`) that never got redirects: `/blog` (page removed, content now lives at `/chaotic-confluence`) and `/services/ai-organizational-model` (page removed, closest living equivalent `/services/ai-integration`) — both added to `next.config.ts` `redirects()` as permanent redirects, live-confirmed 404 before the fix. `/newsletter` is already 200 live; GSC's "last crawled" (3/31) just predates re-verification, no code change. Remaining 29 URLs (22 `companies-nobg/*.png-N` and `companies/*.png-N` malformed image paths, 1 hashed `.woff2`, 1 `/webinars-%26-offerings`) traced through the full git history of every component that renders those images (`Results.tsx`, `services/page.tsx`, `implementation-support/page.tsx`) — none of them, past or present, ever construct a URL with an index suffix (the `-${i}` only ever appears on the React `key`, not `src`); not present in `sitemap.xml` either. These correctly 404 live — no code bug, nothing to fix. `webinars-%26-offerings` has no match anywhere in this repo's git history (including pre-redesign deleted files); left unresolved rather than guessing a redirect target with no evidence, per the addendum's source-hierarchy rule. Tried to identify the external source generating the malformed image-path requests via Vercel Runtime Logs — account is Hobby tier (1-hour retention only, confirmed live in dashboard), so historical requests from Jul 29–Aug 21 aren't recoverable, and live monitoring isn't practical since hits land in sparse clusters rather than continuously; abandoned as not worth the ongoing effort, especially since GSC's own index history (see OPEN-008) suggests Google may have first discovered these before this repo's visible history anyway, meaning there may be no live external link left to find. Follow-up (same session): added `middleware.ts` regex `/^\/companies(-nobg)?\/.+-\d+$/`, matched via `config.matcher` on `/companies/:path*` + `/companies-nobg/:path*`, returning `410 Gone` for any request whose path ends in a literal `-<digits>` suffix under those two folders — a shape none of the 20 real filenames in either `public/companies*` folder ever has. Real logo files (`lum.png`, `RIV.png`, etc.) pass through unaffected; verified against all 4 known-bad GSC examples plus all 20 real filenames. Doesn't stop whatever external source is generating the requests, but should make Google drop each individual URL from its recheck queue faster than a plain 404 does, per Google's own 410-vs-404 handling. | `npx tsc --noEmit` (pass), `npx eslint next.config.ts middleware.ts` (pass), `npm run build` (pass, `/services/ai-integration` and `/chaotic-confluence` build as expected, middleware compiles as `ƒ Proxy (Middleware)`), live `curl` checks of all disputed URLs against production before diagnosing (`/blog`, `/services/ai-organizational-model`, `/newsletter`, `/webinars-%26-offerings`, one `companies-nobg/*.png-N` sample, the `.woff2`, `robots.txt`, `sitemap.xml`), Node regex test of the middleware pattern against all 4 known-bad GSC URLs (all match) and all 20 real filenames in both `public/companies*` folders (none match) | Not deployed — pending commit/review | After deploy: (1) use "Validate Fix" on the `/blog` and `/services/ai-organizational-model` 404 groups; (2) spot-check one `companies-nobg/*.png-N` URL returns 410 in production; (3) `/webinars-%26-offerings` still unresolved — no evidence of a real target, left as-is |
| TECH-005 (new) | DONE | `a06f647` | Claude | | 2026-09-04 | First API-based GSC technical health pull (`GSC_HEALTH_2026-09-04.md`), now that DAT-005's GSC half is live. Confirmed via URL Inspection API: `sc-domain:bbtx.ai` is a Domain property (covers www/apex/http in one), search-performance history begins 2026-03-22, sitemap first submitted 2026-03-23 — **resolves OPEN-008** (no mystery earlier property; just a pre-project sitemap submission). Found `/team/grant` "unknown to Google" (never crawled — exactly 1 internal link sitewide, buried in the `/about` team grid; the homepage's schema `@id` reference doesn't count as a link). Fixed with two real links: `Grant Tate → /team/grant` added to `Footer.tsx`'s "Useful Links" (renders sitewide, ~1→~30 inbound links) and a contextual "Read Grant Tate's full background →" link in `app/about/page.tsx` right after the career-timeline section. Also found `/services/organizational-ai-assessment` 404s live but is still GSC-indexed and drawing ~49 impressions/120d at ~#35 for "organizational ai assessment" — added a 301 in `next.config.ts` to `/services/organizational-assessment` (not `/services/ai-integration`, to avoid reinforcing an AI-category framing per DEC-001), matching the TECH-004 redirect pattern. Also confirmed: canonical consolidation to `www` is working on every indexed page checked; `BreadcrumbList` rich results live on service/team/whitepaper/chaotic-confluence pages; the 14-URL "Page with redirect" GSC report is stale pre-consolidation apex/http addresses correctly *not* indexed, not a defect | `npx tsc --noEmit` + `npm run build` (52/52) clean; `eslint` clean on touched files (1 pre-existing `no-html-link-for-pages` error on `Footer.tsx`'s wordmark, confirmed via `git stash` unrelated) | Committed `a06f647` | (1) **Done 2026-09-04** — Mel flipped the apex `bbtx.ai`→`www.bbtx.ai` redirect to 308 permanent in Vercel, live-verified (`curl -sI https://bbtx.ai/` → `308`); (2) after deploy: "Validate Fix" on `/blog` and `/services/organizational-ai-assessment` groups, request indexing for `/team/grant` and `/services/executive-recruitment`, resubmit sitemap; (3) service-page links to `/team/grant` (report NAV-001) deliberately deferred to AUT-002 rather than bolted on now |
| AEO-003 | DONE | (docs only) | Claude | | 2026-09-04 | Checked whether GSC surfaces generative/AI-Overview reporting, now that access exists. The API's `searchAppearance` dimension is queryable but returns zero rows for `sc-domain:bbtx.ai` over Jun–Sep 2026 (confirmed the property has live data in the same window via `query`, so this isn't an access/data gap) — no query has earned a rich-result/AI-Overview categorization yet. Consistent with AEO-002's 0/8 finding, not new information, but closes the task | N/A — read-only API check |
| AEO-002 | DONE (first pass) | (uncommitted) | Claude | | 2026-09-04 | First AEO/AI-visibility baseline (`AEO_BASELINE_2026-09-04.md`) — 10 buying-intent prompts from the report's Appendix A run via `WebSearch`/`WebFetch` as a retrieval-layer proxy (not queried against ChatGPT/Gemini/Perplexity directly; method documented in the doc). Result: **BBTx named in 0 of 8 non-branded discovery prompts**; one stale/buried mention. Root cause: entity fragmentation — the practice appears under **6 different names** across the public web (bbtx.ai, Chamber profile, Cvillepedia, paradigmassociates.us, Daily Progress archive, directory aggregators), several pointing at the wrong URL (Chamber links to a Circle.so community, not bbtx.ai) or the wrong company name entirely. The branded-query AI synthesis stated outright *"BBTx is an AI business consulting firm."* Directly motivated the ENT-001/002/003/GBP-002 draft corrections shipped the same session (see ENT-005 row below). Recurring monthly re-run still pending — prompt list finalized in the doc for next pass | N/A — research/analysis only, no code touched |
| ENT-005 (new) | DONE | (uncommitted) | Claude | | 2026-07-31 | Homepage `Organization` schema (`app/page.tsx`) gained `address` (Charlottesville, VA, US — matches already-live Nav.tsx copy and Grant's existing `homeLocation`), `founder` (linked to Grant's `Person` node via new `@id` on `app/team/grant/layout.tsx`), and `sameAs` (Grant's LinkedIn, Substack, Medium — all already-published links reused from `app/components/Footer.tsx`, nothing new asserted). `app/components/Footer.tsx` also gained a visible, unconditionally-rendered "Charlottesville, VA, USA" line — the only prior location text (`Nav.tsx` mobile menu) was gated behind `{mobileOpen && ...}` and never appeared in server-rendered HTML |
| POS-002 | NOT STARTED | | Claude + Mel | H-005 (OPEN-001) | | Draft homepage copy rewrite for wording review |
| AUT-001/AUT-002 | IN PROGRESS | | Grant + Mel + Claude | H-006 | 2026-07-30 | Public-footprint research done instead of blind interview (`AUT-001_RESEARCH_BRIEF.md`) — real public material found (CATEC, Project Rebound, Chamber seat, 2 books, Chaotic Confluence), plus 5 factual conflicts only Grant can resolve (see brief). Nothing publishes until he confirms — H-006 still applies |
| GBP-001 | DONE | | Mel + Grant | | 2026-09-04 | Manager access confirmed working — Mel edited the live listing directly 2026-09-04 |
| GBP-002 | IN PROGRESS | | Mel + Grant | H-009 (partial — veteran/women-owned attributes) | 2026-09-04 | **Applied live 2026-09-04:** description rewritten (dropped keyword-stuffed original), primary category set to `Business management consultant` + secondary `Business development service` added, LinkedIn company URL added as a social profile. **Still open:** website `http://` → `https://` fix, remove old-entity socials (`facebook.com/thebridgeltd`, `x.com/@thebridgeltd`), service area (`United States` → 7 named Central VA counties + hide the `184 Brookwood Dr` address as a service-area business), hours (`Open 24 hours` → Mon–Fri 9–5), and veteran-/women-owned attributes need Grant to confirm as literally true before they stay set. Full spec in `ENT_PROFILE_DRAFTS_2026-09-04.md` §3 |
| CAS-001/002 | NOT STARTED | | Grant + Mel + Claude | H-007 | | Candidates already named in code: Lincoln Surveying, InBio |
| ENT-001 | IN PROGRESS | | Mel + Grant | H-008 | 2026-09-04 | Canonical entity block drafted (`ENT_PROFILE_DRAFTS_2026-09-04.md` §0) — name, website, founder line, phone, location, service area, one-line descriptor, ~60-word standard description, specialties list, all meant to be reused verbatim across every off-site profile. Several fields marked `CONFIRM` pending Grant (exact legal name/DBA, phone currency, founder-vs-co-founder) — see new `OPEN_DECISIONS.md` OPEN-011 |
| ENT-002 | NOT STARTED | | Mel | OPEN-005 (Chamber login) | 2026-09-04 | Corrected copy fully drafted and paste-ready (`ENT_PROFILE_DRAFTS_2026-09-04.md` §1): fixes the wrong website (currently a Circle.so community, not bbtx.ai), removes the live "AI Explorers" membership checkout link, rewrites the description, and trims 8 categories to 4 accurate ones. Highest-impact remaining off-site fix — nothing left but the member-dashboard login |
| ENT-003 | IN PROGRESS | | Mel + Grant | OPEN-005 (LinkedIn company-page admin access) | 2026-09-05 | Company page **found** 2026-09-04: `linkedin.com/company/bridgebusinesstransformations/` (37 followers, name already "BBTx Consulting", but URL slug/tagline/cover image are old AI-first branding). Grant is not admin on the company page — likely admin is a dormant "R. Grant" account. Mel filed a LinkedIn admin-access request 2026-09-04. **"BBTx Consulting" added as current experience on both Grant's and Mel's own personal LinkedIn profiles** 2026-09-04 — the step `ENT_PROFILE_DRAFTS_2026-09-04.md` §2 calls out as the fastest path to the admin claim resolving, since LinkedIn uses employment as proof. Company-page copy rewrite (§2) still queued for admin access. **Scope note, 2026-09-05:** the drafted personal-profile rewrite (§2a — headline/About/Featured) is explicitly **out of scope**, Mel's call — Grant's personal profile stays his own decision, not a project deliverable; the doc keeps it only as a reference if he ever wants it, untracked. **Do not create a new company page** — this one carries the followers and Google's existing cross-reference |
| ENT-004 | NOT STARTED | | Mel | H-008, H-009 | | Legacy records/author profiles/speaker bios not yet audited |
| AEO-004 (new) | DONE | (uncommitted) | Claude | | 2026-09-03 | Added `public/llms.txt` — a Markdown site map for AI/LLM crawlers per the llmstxt.org convention. Summary blockquote is DEC-001's approved positioning statement verbatim; all page descriptions reused verbatim from each page's own existing, already-approved metadata (no new claims); deliberately omits the unverified "100+ organizations" figure (OPEN-001) and does not enumerate individual blog posts (would go stale — links to the `/chaotic-confluence` hub instead). `robots.ts` untouched — already `allow: "/"` for all user agents, so no AI-crawler-specific rule was needed. Prompted by a Reddit post + AI-marketing listicle Mel shared claiming `llms.txt` and "Quick Answer" content blocks drove another (unrelated) site's AI-citation growth; most of that post's tactics were already covered by this project's existing backlog (schema, canonical domain, image/LCP work, GSC query-gap analysis) or were inapplicable/inadvisable (a "5-site programmatic flywheel," which conflicts with the addendum's thin-content rule; the post's IndexNow claim is also technically wrong — Google doesn't consume IndexNow, only Bing/Yandex/Seznam do). Quick-Answer/question-phrased-H2 formatting was **not** retrofitted onto existing pages (no report basis for a copy rewrite) — logged as a future `CNT-001` editorial-checklist item instead | `npx tsc --noEmit` clean (no code touched, static file only). `npm run build`/`lint` not run this session — pure static-asset addition, no risk surface for either |
| TST-001 (new) | READY FOR REVIEW | (uncommitted) | Claude + Mel | | 2026-08-20 | Client testimonial submission flow (Grant's idea, forwarded by Mel) — not report-driven, a direct site request. New Neon table `testimonial_submissions` (applied live via `psql`, see `lib/neon-schema.sql`), public `POST /api/testimonials` (zod validation, honeypot, per-IP rate limit, Resend notification to Grant) + `POST /api/testimonials/photo-upload` (Vercel Blob, JPEG/PNG/WebP only, 5MB cap), `app/components/TestimonialSubmissionModal.tsx` (Name / Role-Company / testimonial prompted with "What changed for you or your organization after working with Grant?" / optional photo / required permission-to-publish checkbox), wired into a new CTA button on `app/sections/Testimonials.tsx`, admin moderation queue at `/admin/testimonials` (`lib/admin/{types,queries}.ts` additions, `components/admin/AdminSidebar.tsx` nav entry) — inherits `/admin/*` auth from existing `middleware.ts`, no new auth code. Submissions do **not** auto-publish; the live testimonial wall stays the hand-curated array in `Testimonials.tsx`, same as today. Also added Sayeh Agah's testimonial (forwarded by Grant, permission established in his original ask + her reply) to that array as the first real case. `npm run build` + scoped `eslint` on every new/touched file pass clean; only pre-existing issues remain (confirmed by identical unfixed pattern already in `AssessmentDetailSheet.tsx`/`Nav.tsx`/`AdminSidebar.tsx`, unrelated to this change). Not visually reviewed in-browser this session (user preference — Mel reviews UI changes visually rather than via automated screenshot). **CTA button copy ("Share Your Experience") is a placeholder** — Grant is choosing the final wording from 4 options; one-line swap in `TestimonialSubmissionModal.tsx` + `Testimonials.tsx` once decided. |
| SVC-006 (new) | READY FOR REVIEW | (uncommitted) | Claude + Mel | | 2026-08-20 | Executive Recruitment Assessment Services page — not report-driven, Grant emailed the copy directly (see his "Executive Recruitment Package" email, 2026-08-18) asking only that it be placed on the site tonight; structure/conversion optimization and full SEO/AEO pass explicitly deferred to a later session per Mel. New `app/services/executive-recruitment/{page,layout}.tsx` (metadata + `Service`/`BreadcrumbList` JSON-LD, matching the pattern of the other 5 service pages) using Grant's copy verbatim/near-verbatim (assessment dimensions, 6-step process, what's included, benefits, appropriate uses, advisory-tool disclaimer) — no invented proof sections, no testimonial tie-in, no FAQ (none of the 6 existing testimonials or FAQ items are about executive recruitment, so none were fabricated). Linked from `Nav.tsx`'s Services dropdown and `app/sitemap.ts`. Deliberately **not** added to the homepage `Services.tsx` 4+1 grid or `/services` page's 5-stage narrative — both are copy-locked around "five" services and changing that is the structure/conversion work Mel asked to defer. `npm run build` + scoped `eslint` pass clean (4 unescaped-apostrophe errors introduced by this page's own copy were fixed inline). |
| CRS-001 (new) | READY FOR REVIEW | (uncommitted) | Claude + Mel | | 2026-08-08 | New `/courses` page shipped (product/monetization task, not report-driven — 8 Gumroad-only products now have a home on bbtx.ai). Built via `/impeccable init` → `shape` → `craft`: hero, Making Modern Managers 3-tier ladder (Self-Paced $499 / Group Cohort $999 "coming soon" + waitlist / Individual Coaching $3,500), 5 AI-course cards (real Gumroad-derived copy), 2 credibility placements, FAQ, closing CTA. New files: `app/courses/page.tsx`, `app/courses/layout.tsx` (metadata + BreadcrumbList/FAQPage JSON-LD), `app/courses/courses-faq-data.ts`, `app/components/WaitlistModal.tsx` (mirrors `SubscribeModal.tsx`, posts to `/api/newsletter` with `source: "course-cohort-waitlist"`, no new backend). Edited: `app/coaching/page.tsx` (removed `PLANS` pricing cards + dead Group Cohort Gumroad CTA, replaced with a pointer card to `/courses`), `app/coaching/coaching-faq-data.ts` (2 answers updated to match), `app/components/Nav.tsx` + `app/components/Footer.tsx` (Courses added under Resources; Footer's old raw Gumroad-storefront link now points on-site), `app/sitemap.ts` (+`/courses`). Real bugs found and NOT reproduced on-site: Self-Paced/Group Cohort Gumroad pages carry duplicated Individual-Coaching body copy (wrote fresh accurate copy instead); 3 AI-course pages cross-sell 2 non-existent course names (dropped); 3 of 6 leftover `public/*.png` course-cover images from an earlier 6-course lineup don't match any current title (used only the 3 that do: `ai4leadership.png`, `bizval.png`, `aiempcoach.png`; fixed an aspect-ratio/flex-shrink bug that was cropping their baked-in titles) — see `OPEN_DECISIONS.md` OPEN-010. `npx tsc --noEmit` + `npx eslint` + `npm run build` all pass (1 pre-existing unrelated `no-html-link-for-pages` error in `Nav.tsx`/`Footer.tsx`, confirmed via `git diff` untouched). Desktop screenshots reviewed end-to-end (hero, both card grids, FAQ, closing CTA, `/coaching` pointer, waitlist modal); mobile viewport screenshots could not be captured this session (`resize_window` tool did not affect actual render viewport despite repeated attempts — a tool limitation, not a code issue) — mobile uses the same responsive Tailwind breakpoint classes as the already-live `/coaching`/homepage patterns verbatim, but has not been visually confirmed. No dedicated `impeccable-finish-reviewer` subagent is registered in this environment; substituted a self-review pass against `craft-floor.md`'s Refuse list and the shape brief's coverage checklist (disclosed per the skill's degraded-path rule). One deliberate floor deviation: kept the site's existing eyebrow-label convention (used on every other section sitewide) rather than the skill's generic ban, since the user's brief explicitly required matching the incumbent designer's system. Not yet committed. Manual follow-ups: Mel to supply real cover images for the 2 AI courses currently showing a placeholder icon (Generative AI for Coaches and Consultants; Building the Organizational Digital Twin); Grant to fix the 2 Gumroad listing bugs directly; mobile visual QA still pending. |

## Decisions

| Date | Decision | Evidence | Approver | Revisit condition |
|---|---|---|---|---|
| 2026-07-28 | Approved primary positioning statement (DEC-001) | Report pp. 2, 20, 33; this session's audit confirming homepage still AI-first | Grant + Mel | Reassess no earlier than ~2027-01-28 (6 months of query/lead/client evidence), per report p. 29 |
| 2026-07-28 | Keep existing service-page URL slugs (`/services/strategy-advisory` etc.) rather than migrating to the report's suggested slugs (URL-001) | Slugs are live, indexed, and already carry the recommended content framing | Mel (technical call) | Revisit only if Search Console data shows the current slugs are underperforming |

## Completed changes

| Date | Task ID | Files/routes changed | Validation performed | Deployment result | Follow-up |
|---|---|---|---|---|---|
| 2026-07-28 | GOV-001 | `CLAUDE.md` (new), `docs/seo-aeo/**` (new) | N/A (docs only) | Not deployed — pending commit | None |
| 2026-07-28 | GOV-002/DAT-004 | `docs/seo-aeo/CURRENT_STATE_AUDIT.md`, `REPORT_TRACEABILITY_MATRIX.md`, `REPO_IMPLEMENTATION_BACKLOG.md`, `OPEN_DECISIONS.md`, `MANUAL_GATES.md` (new) | N/A (docs only) | Not deployed — pending commit | Mel/Grant review before Phase 1 work begins |
| 2026-07-28 | TECH-001 | `app/{privacy,terms,cookies,resources,links}/page.tsx` (canonical+OG), `app/sitemap.ts` (+`/resources`,+`/links`), `app/services/*/layout.tsx` (`Service`+`BreadcrumbList`), `app/team/*/layout.tsx` (`Person`+`BreadcrumbList`, sourced from `team-data.ts`), `app/whitepapers/layout.tsx`, `app/chaotic-confluence/{layout,page,[slug]/page}.tsx` (`BreadcrumbList`, deduplicated so hub and post pages each emit exactly one), `app/coaching/{layout,page}.tsx` (`BreadcrumbList`+`FAQPage`), `app/sections/{FAQ,faq-data}.tsx` + `app/page.tsx` (`FAQPage` sourced from the same array FAQ.tsx renders), `app/page.tsx` (removed non-functional `SearchAction`/`SiteLinksSearchBox` — no site search feature exists) | `npm run build` (pass), `npx eslint` on all touched files (pass — 1 pre-existing unrelated error in `links/page.tsx` confirmed via `git diff`), manual `curl` spot-checks of sitemap entries and JSON-LD on homepage/services/team/coaching/whitepapers/chaotic-confluence (hub + post) | Not deployed — pending commit/review | None blocking; `Organization.sameAs`/`LocalBusiness` remain open pending ENT-001/H-002 |
| 2026-07-30 | DAT-003 | `docs/seo-aeo/BASELINE_2026-07-30.md` (new), `OPEN_DECISIONS.md` (+OPEN-008, +OPEN-009), `REPO_IMPLEMENTATION_BACKLOG.md` (+TECH-002, DAT-001/003/005 status) | Manual review of Mel's GSC Coverage + Performance CSV exports; live `curl -I` checks of apex/www/http/https redirect behavior; `vercel project ls`/`vercel domains ls` (read-only) to confirm production domain | Docs only — no deploy | Surfaced TECH-002 (canonical domain mismatch, blocked on OPEN-009) and OPEN-008 (GSC property predates project — needs Mel's confirmation) |
| 2026-07-31 | TECH-002 | 29 files: `app/layout.tsx`, `app/sitemap.ts`, `app/robots.ts`, `app/page.tsx`, every route `layout.tsx`/`page.tsx` with canonical+OG (about, privacy, terms, cookies, resources, links, newsletter, digital-twin-snapshot, coaching, whitepapers, chaotic-confluence + `[slug]`, services + all 5 service pages, team/grant+kaye+mel), 4 `lib/emails/*.ts` templates | `npx tsc --noEmit` (pass), `npx eslint` on every touched file (pass — 1 pre-existing unrelated error in `links/page.tsx`, confirmed via `git diff` untouched by this change), `grep -r "https://bbtx.ai"` repo-wide confirms zero remaining apex references and no accidental `www.www.` doubling | Not deployed — pending commit/review | None blocking |
| 2026-07-31 | ENT-005 / AUD-001 | `app/page.tsx` (Organization `address`+`founder`+`sameAs`), `app/team/grant/layout.tsx` (Person `@id`), `app/components/Footer.tsx` (visible location line) | `npx tsc --noEmit` + `npx eslint` on all 3 files (pass — same pre-existing `links/page.tsx` error only, confirmed unrelated) | Not deployed — pending commit/review | Chamber/LinkedIn company-page `sameAs` entries still excluded pending OPEN-005 (only Grant's personal, already-published profile links used) |
| 2026-08-01 | TECH-003 | `app/sections/Services.tsx` (5 service names `<p>`→`<h3>`), `app/sections/Advantage.tsx` (heading hierarchy corrected — see Active work row above), `app/sitemap.ts` (removed fabricated `lastModified` from 20 static entries), `app/sections/Results.tsx` (removed `unoptimized` from 38 logo `<Image>` renders) | `npx tsc --noEmit` (pass), `npx eslint` on all 4 files (pass — 1 pre-existing unrelated apostrophe-escaping error in `Advantage.tsx`, confirmed via `git diff` same string, unrelated to the tag change) | Not deployed — pending commit/review | None blocking — purely structural/technical, no visible copy or design changed |
| 2026-09-04 | TECH-005 | `app/components/Footer.tsx` (+`/team/grant` link), `app/about/page.tsx` (+contextual link), `next.config.ts` (+301 `/services/organizational-ai-assessment`→`/services/organizational-assessment`), `docs/seo-aeo/GSC_HEALTH_2026-09-04.md` (new) | First API-based GSC pull via Search Console MCP (URL Inspection + sitemap + search-analytics APIs); `npx tsc --noEmit` + `npm run build` (52/52) clean; `eslint` clean on touched files (1 pre-existing unrelated error, confirmed via `git stash`) | Committed `a06f647` | Vercel apex-redirect 307→308 change (Mel); post-deploy GSC "Validate Fix" + reindex requests + sitemap resubmit |
| 2026-09-04 | AEO-002 | `docs/seo-aeo/AEO_BASELINE_2026-09-04.md` (new) | Manual review of 10 `WebSearch`/`WebFetch` prompt runs against the report's Appendix A prompt set | Docs only — no deploy | Directly motivated ENT-001/002/003/GBP-002 draft corrections (same session); recurring monthly re-run pending |
| 2026-09-04 | GBP-002 (partial) | Google Business Profile (external — no repo files) | Manual verification in the GBP dashboard by Mel | Live on Google (external) | 4 corrections + veteran/women-owned confirmation still open — see Active work row |

## Open risks

| Risk | Impact | Control | Owner | Status |
|---|---|---|---|---|
| Category inconsistency | Search and buyer confusion | Position approved (DEC-001); hold for ≥6 months unless evidence demands change | Grant + Mel | Mitigated — position set, homepage copy still pending (POS-002) |
| Unsupported claims ("100+ organizations") | Credibility/legal risk | H-005 evidence gate (OPEN-001) | Grant + Mel | Open — scope corrected 2026-07-31, now confirmed live in 9 files, not 2 |
| Analytics overwrite | Data loss | N/A — confirmed no existing analytics to overwrite; all GA4/GTM work is greenfield | Mel | Resolved (no risk exists) |
| URL migration errors | Traffic/indexation loss | Decided: no URL migration planned (URL-001) | Mel | Resolved |
| Schema/claims drift | New JSON-LD accidentally asserting unverified facts | TECH-001 scoped to only mirror existing visible copy; `sameAs`/`LocalBusiness` explicitly excluded pending ENT-001/H-002 | Claude | Controlled |
| Canonical domain split (`bbtx.ai` vs `www.bbtx.ai`) | Search signal/authority split across two hostnames; likely source of 6 open GSC canonical/redirect issues | Mel confirmed `www.bbtx.ai`; code updated repo-wide 2026-07-31 (OPEN-009 resolved) | Mel | **Resolved** — 2026-09-04 GSC health pull confirmed consolidation is actively working (Google-selected canonical = declared canonical on every indexed page checked). Apex `https` redirect flipped 307→308 permanent in Vercel by Mel 2026-09-04, live-verified (`curl -sI https://bbtx.ai/` → `308` → `https://www.bbtx.ai/`). Monitor GSC's 14-URL "Page with redirect" report for it to clear on its own now that Google isn't re-checking a temporary redirect |
| Entity fragmentation across public web (6 different names/URLs for the same practice) | AI answer engines and search synthesize the wrong category and can't consistently resolve "who is BBTx" — 2026-09-04 AEO baseline found 0/8 non-branded discovery prompts named BBTx, and one AI synthesis stated outright "BBTx is an AI business consulting firm" | ENT-001 canonical entity block + ENT-002/003/GBP-002 corrected copy all drafted 2026-09-04; GBP corrections partially live; Chamber/LinkedIn gated on account access (OPEN-005) | Mel + Grant | Open — see `AEO_BASELINE_2026-09-04.md`, `ENT_PROFILE_DRAFTS_2026-09-04.md`, `OPEN_DECISIONS.md` OPEN-011 |
