# Quick-start prompt

Use this only after the report text and addendum are inside the repository.

```text
Read the BBTx SEO/AEO report and project instructions in docs/seo-aeo in full. Do not edit code yet. Inspect the repository, prove comprehension with a section-by-section traceability matrix, then create a repository-specific backlog that classifies every task as (A) directly implementable in code, (B) hybrid requiring credentials/approval/external action, or (C) fundamentally human/relationship-driven. Include dependencies, manual gates, exact files/routes, acceptance criteria, tests, risks, and rollback. Identify contradictions between the report and the current site. Only after I approve the backlog may you implement one unblocked task at a time. Never invent claims, client results, reviews, credentials, or local evidence; never change URLs without a redirect/canonical/sitemap plan; never overwrite analytics before auditing it; and keep Grant's judgment as the product with AI as a supporting tool.
```
